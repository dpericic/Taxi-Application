import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    icon: {
      color: "#fff",
      fontSize: 25
    },
    headerText: {
      color: "#fff",
      fontSize: 25,
      justifyContent: "center"
    },
    textWrapper: {
      flex: 1,
      alignItems: "center",
      justifyContent: "center"
    },
    aboutText: {
      fontSize: 16
    }
  });